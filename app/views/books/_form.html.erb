<div class = "container">
<%= form_with(model: book, local: true) do |form| %>
  <% if book.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(book.errors.count, "error") %> prohibited this book from being saved:</h2>

      <ul>
      <% book.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group m-2">
    <%= form.text_field :name,class:'form-control',placeholder:'Name' %>
  </div>

  <div class="form-group m-2">
    <%= form.text_field :author, class:'form-control',placeholder:'Author'%>
  </div>

  <div class="form-groupm m-2">
    <%= form.text_field :publisher,class:'form-control',placeholder:'Publisher'%>
  </div>

  <div class="form-group m-2">
    <%= form.number_field :pages,class:'form-control',placeholder:'Pages' %>
  </div>

  <div class="form-group m-2">
    <%= form.submit class:'btn btn-primary' %>
  </div>
<% end %>
</div>